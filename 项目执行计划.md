# 副业发现平台 - 项目执行计划

> 创建时间：2026年4月23日
> 执行策略：质量优先，稳步推进
> 预计周期：4周

---

## 执行原则

1. **每个任务有明确产出**：不接受模糊的"完成"，必须有具体交付物
2. **每个产出有质量检查**：自己先验证，再提交审核
3. **每个阶段有复盘优化**：发现问题立即修正，不遗留技术债
4. **文档先行**：先设计再执行，避免返工

---

## 第一阶段：数据完善（第1-2周）

### 1.1 案例数据收集

#### 任务清单
| 任务ID | 任务内容 | 目标数量 | 负责人 | 状态 | 质量标准 |
|--------|---------|---------|--------|------|---------|
| D001 | 任务型案例收集 | 100条 | 子任务 | 待开始 | 每条必填10个字段，来源可追溯 |
| D002 | 配送型案例收集 | 100条 | 子任务 | 待开始 | 同上 |
| D003 | 设计类案例收集 | 100条 | 子任务 | 待开始 | 同上 |
| D004 | 技术类案例收集 | 100条 | 子任务 | 待开始 | 同上 |
| D005 | 写作类案例收集 | 100条 | 子任务 | 待开始 | 同上 |
| D006 | 视频直播类案例收集 | 100条 | 子任务 | 待开始 | 同上 |
| D007 | 电商类案例收集 | 100条 | 子任务 | 待开始 | 同上 |
| D008 | 失败被骗案例收集 | 100条 | 子任务 | 待开始 | 同上 |

#### 数据质量标准
每条案例必须包含：
- ✅ 用户画像（身份、时间、技能）
- ✅ 平台名称
- ✅ 月收入（具体数字或范围）
- ✅ 投入时间
- ✅ 经历详情（100字以上）
- ✅ 困难与解决
- ✅ 新人建议
- ✅ 数据来源（可追溯）
- ✅ 验证状态
- ✅ 收集时间

#### 数据验证流程
1. 子任务收集 → 初步整理
2. 格式校验（必填字段完整性）
3. 内容审核（真实性、合理性）
4. 来源追溯（链接/截图佐证）
5. 入库保存

---

### 1.2 平台数据核实

#### 任务清单
| 任务ID | 任务内容 | 产出 | 状态 |
|--------|---------|------|------|
| P001 | 现有60个平台状态核实 | 更新后的platforms.json | 待开始 |
| P002 | 新增热门平台调研 | 新平台补充 | 待开始 |
| P003 | 平台投诉数据收集 | complaints.json | 待开始 |

#### 核实标准
- 官网能否访问
- APP是否在应用商店
- 近期用户评价（知乎/小红书/黑猫投诉）
- 是否有停运/跑路新闻

---

### 1.3 骗局数据补充

#### 任务清单
| 任务ID | 任务内容 | 目标数量 | 状态 |
|--------|---------|---------|------|
| S001 | 骗局案例收集 | 100条 | 待开始 |
| S002 | 骗局识别方法整理 | 50种 | 待开始 |

#### 数据来源
- 公安部反诈中心通报
- 央视每周质量报告
- 黑猫投诉平台
- 用户投稿

---

## 第二阶段：技术架构升级（第3周）

### 2.1 Supabase配置

#### 任务清单
| 任务ID | 任务内容 | 产出 | 状态 |
|--------|---------|------|------|
| T001 | Supabase项目创建 | 项目ID | 待开始 |
| T002 | 数据库表设计 | SQL Schema | 待开始 |
| T003 | API接口配置 | RESTful API | 待开始 |
| T004 | 用户认证配置 | Auth配置 | 待开始 |

#### 数据库表设计
```sql
-- 用户表
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  email VARCHAR(255) UNIQUE,
  nickname VARCHAR(50),
  created_at TIMESTAMP DEFAULT NOW(),
  level VARCHAR(20) DEFAULT 'normal',
  points INTEGER DEFAULT 0
);

-- 案例表
CREATE TABLE cases (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_nickname VARCHAR(50),
  city VARCHAR(50),
  platform_id UUID REFERENCES platforms(id),
  category VARCHAR(50),
  monthly_income INTEGER,
  daily_hours VARCHAR(20),
  experience TEXT,
  difficulties TEXT,
  suggestions TEXT,
  income_curve JSONB,
  source_url VARCHAR(500),
  verified BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMP DEFAULT NOW()
);

-- 平台表
CREATE TABLE platforms (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  name VARCHAR(100),
  type VARCHAR(50),
  website VARCHAR(255),
  status VARCHAR(20), -- active/warning/stopped/danger
  income_range JSONB,
  description TEXT,
  pros TEXT[],
  cons TEXT[],
  verified_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT NOW()
);

-- 平台评价表
CREATE TABLE platform_reviews (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES users(id),
  platform_id UUID REFERENCES platforms(id),
  rating INTEGER CHECK (rating >= 1 AND rating <= 5),
  content TEXT,
  created_at TIMESTAMP DEFAULT NOW()
);

-- 投诉表
CREATE TABLE complaints (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES users(id),
  platform_id UUID REFERENCES platforms(id),
  type VARCHAR(50),
  content TEXT,
  status VARCHAR(20) DEFAULT 'pending', -- pending/processing/resolved/rejected
  reply TEXT,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP
);

-- 骗局表
CREATE TABLE scams (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  name VARCHAR(100),
  type VARCHAR(50),
  description TEXT,
  methods TEXT[],
  warning_signs TEXT[],
  real_cases JSONB,
  created_at TIMESTAMP DEFAULT NOW()
);

-- 收藏表
CREATE TABLE favorites (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES users(id),
  platform_id UUID REFERENCES platforms(id),
  created_at TIMESTAMP DEFAULT NOW(),
  UNIQUE(user_id, platform_id)
);
```

---

### 2.2 前端改造

#### 任务清单
| 任务ID | 任务内容 | 产出 | 状态 |
|--------|---------|------|------|
| F001 | 用户注册/登录页面 | auth.html | 待开始 |
| F002 | 个人中心页面 | profile.html | 待开始 |
| F003 | 平台评价功能 | 集成到详情页 | 待开始 |
| F004 | 投诉提交功能 | complaint.html | 待开始 |
| F005 | 案例投稿功能 | 优化submit.html | 待开始 |
| F006 | Supabase SDK集成 | supabase.js | 待开始 |

---

### 2.3 管理后台

#### 任务清单
| 任务ID | 任务内容 | 产出 | 状态 |
|--------|---------|------|------|
| A001 | 后台首页（数据概览） | admin/index.html | 待开始 |
| A002 | 案例管理页面 | admin/cases.html | 待开始 |
| A003 | 平台管理页面 | admin/platforms.html | 待开始 |
| A004 | 用户管理页面 | admin/users.html | 待开始 |
| A005 | 投诉管理页面 | admin/complaints.html | 待开始 |

---

## 第三阶段：功能完善与测试（第4周）

### 3.1 功能测试

#### 测试清单
| 测试ID | 测试内容 | 通过标准 | 状态 |
|--------|---------|---------|------|
| TEST01 | 用户注册流程 | 3分钟内完成注册 | 待开始 |
| TEST02 | 案例投稿流程 | 投稿成功+入库 | 待开始 |
| TEST03 | 平台评价功能 | 评价显示正确 | 待开始 |
| TEST04 | 投诉提交功能 | 投诉入库+状态正确 | 待开始 |
| TEST05 | 数据搜索功能 | 搜索结果准确 | 待开始 |
| TEST06 | 移动端适配 | 功能正常使用 | 待开始 |
| TEST07 | 管理后台功能 | CRUD操作正常 | 待开始 |

---

### 3.2 性能优化

- 图片压缩与懒加载
- 数据分页加载
- API请求优化
- 缓存策略

---

### 3.3 安全加固

- 用户输入校验
- XSS防护
- SQL注入防护
- 敏感数据加密

---

## 第四阶段：上线与运营（持续）

### 4.1 上线部署

- 域名配置
- HTTPS证书
- 监控告警

### 4.2 运营准备

- SEO优化
- 内容更新机制
- 用户反馈收集

---

## 进度追踪

### 本周目标（第1周）
- [ ] 完成8类案例收集（每类先完成30条作为样板）
- [ ] 完成平台数据核实
- [ ] 完成数据库表设计
- [ ] 完成Supabase项目创建

### 每日检查
每天记录进度和问题，确保按时推进。

---

## 质量检查清单

### 数据质量
- [ ] 每条案例10个必填字段完整
- [ ] 每条案例有来源佐证
- [ ] 收入数据合理（不过高也不过低）
- [ ] 内容真实可信（有细节有困难有建议）

### 功能质量
- [ ] 每个功能可正常使用
- [ ] 错误提示友好
- [ ] 无明显BUG
- [ ] 移动端适配良好

### 代码质量
- [ ] 代码有注释
- [ ] 变量命名规范
- [ ] 无冗余代码
- [ ] 性能可接受

---

## 风险与应对

| 风险 | 可能性 | 影响 | 应对措施 |
|-----|-------|------|---------|
| 数据收集困难 | 高 | 高 | 先做样板，验证可行性后再批量 |
| Supabase限制 | 中 | 中 | 预留迁移方案 |
| 开发进度延迟 | 中 | 高 | 设置缓冲时间，关键路径优先 |
| 功能BUG多 | 中 | 高 | 每个功能开发完立即测试 |

---

## 下一步行动

**今天要做的**：
1. 启动第一批案例收集（任务型、配送型各30条作为样板）
2. 设计数据库表结构
3. 准备Supabase项目创建
